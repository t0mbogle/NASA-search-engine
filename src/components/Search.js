import React, { useState } from "react";
import "../styles/search.css";

const Search = () => {
    return(
        <>
          <input type="text" className="input" placeholder="search away :)" />
        </>
    );
};

export default Search;